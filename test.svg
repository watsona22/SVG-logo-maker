
        <svg version="1.1"
        width="300" height="200"
        xmlns="svg">
        <shape width="100%" height="100%" fill="white" />
        <function draw() {
    const container = data.shape;


    var canvas = document.getElementById('container');
    if (canvas.getContext) {
        var context = canvas.getContext('2d');

        context.fillRect(20, 20, 100, 100);
        context.clearRect(40, 40, 60, 60);
        context.strokeRect(45, 45, 50, 50);
    }
} cx="150" cy="100" r="80" fill="function shapeColor() {
            if (color2 === 'orange') {
                style.backgroundColor = 'orange'

            } else if (color2 === 'purple') {

                style.backgroundColor = 'purple'
            } else {
                style.backgroundColor = 'teal'
            }
        }" />
        <text x="150" y="125" font-size="60" text-anchor="middle" fill="function letterColor() {
            if (color1 === 'white') {
                style.color = 'white'

            } else if (color2 === 'black') {

                style.color = 'black'
            } else {
                style.color = 'green'
            }
        }">SVG</text>
        </svg>
        {